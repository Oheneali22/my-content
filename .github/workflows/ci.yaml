name: First CI Pipeline

on:
  push:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Show files
      run: ls -la

    - name: Say hello
      run: echo "Hello Ali, GitHub Actions is working"
